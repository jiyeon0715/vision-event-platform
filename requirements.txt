# Runtime dependencies for local development, Docker, and production-like runs.
# CI unit tests install requirements-ci.txt to avoid native vision/tracking builds.
fastapi>=0.110.0,<1
uvicorn[standard]>=0.27.0,<1
pydantic>=2.6.0,<3
PyYAML>=6.0,<7
SQLAlchemy>=2.0.0,<3

# Vision/tracking runtime dependencies.
opencv-python>=4.9.0,<5
ultralytics>=8.0.0,<9
lapx>=0.5.0,<1

# Runtime database client.
psycopg[binary]>=3.1.0,<4

# Local test tools.
pytest>=8.0.0,<9
httpx>=0.27.0,<1
